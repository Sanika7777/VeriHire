{
  "name": "verihire",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @verihire/web dev",
    "build": "pnpm --filter @verihire/web build",
    "lint": "pnpm --filter @verihire/web lint",
    "typecheck": "pnpm --filter @verihire/web typecheck",
    "test": "pnpm --filter @verihire/web test"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
